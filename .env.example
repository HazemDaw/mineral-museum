# Public site URL used to build the stable QR-code links on each mineral page,
# e.g. https://museum.example.ru — printed QR codes only encode this URL + slug,
# never the mineral's data, so info can be edited later without reprinting labels.
NEXT_PUBLIC_SITE_URL=https://museum.example.ru
